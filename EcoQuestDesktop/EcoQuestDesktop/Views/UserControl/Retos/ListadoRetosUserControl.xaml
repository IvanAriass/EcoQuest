<UserControl
    x:Class="EcoQuestDesktop.Views.ListadoRetosUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EcoQuestDesktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:EcoQuestDesktop.ViewModels.Retos"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:ListadoRetosVM />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Margin="0,0,0,10"
            HorizontalAlignment="Center"
            Content="Retos y Puntos Eco"
            FontSize="52" />

        <Border
            Grid.Row="1"
            Width="300"
            Padding="16"
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            Background="#3BB273"
            CornerRadius="16">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="14"
                    Foreground="White"
                    Text="Tus Puntos Eco" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="40"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="{Binding SaldoPuntos}" />
            </StackPanel>
        </Border>

        <ScrollViewer
            Grid.Row="2"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,20">
                <TextBlock
                    Margin="20,0,0,10"
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="Retos disponibles" />
                <ItemsControl ItemsSource="{Binding ListaRetos}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Style="{StaticResource CardWrapperStyle}">
                                <Border Style="{StaticResource CardShadowLayerStyle}" />
                                <Border Style="{StaticResource CardContentLayerStyle}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock
                                            Grid.Row="0"
                                            Style="{StaticResource CardTitleStyle}"
                                            Text="{Binding Nombre}" />

                                        <TextBlock
                                            Grid.Row="1"
                                            Margin="8,4"
                                            FontSize="13"
                                            Foreground="#666"
                                            Text="{Binding Descripcion}"
                                            TextWrapping="Wrap" />

                                        <Border
                                            Grid.Row="2"
                                            Margin="8,4"
                                            Padding="8,4"
                                            Background="#E8F5E9"
                                            CornerRadius="8">
                                            <TextBlock
                                                FontSize="16"
                                                FontWeight="Bold"
                                                Foreground="#2E7D32"
                                                Text="{Binding Puntos, StringFormat='{}{0} pts'}" />
                                        </Border>

                                        <TextBlock
                                            Grid.Row="3"
                                            Margin="8,4,8,8"
                                            FontSize="11"
                                            Foreground="#999"
                                            Text="{Binding FrecuenciaLabel, StringFormat='Frecuencia: {0}'}" />
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <TextBlock
            Grid.Row="3"
            Margin="20,10"
            FontSize="20"
            FontWeight="SemiBold"
            Text="Historial de puntos" />

        <ScrollViewer
            Grid.Row="4"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding HistorialPuntos}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="40,4"
                            Padding="12"
                            Background="White"
                            CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Foreground="#666"
                                    Text="{Binding FechaFormateada}" />
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="12,0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{Binding Concepto}" />
                                <TextBlock
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    FontSize="16"
                                    FontWeight="Bold"
                                    Foreground="{Binding Signo, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}{1}">
                                            <Binding Path="Signo" />
                                            <Binding Path="Puntos" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
